import 'package:flutter/material.dart';

class AppColor {
  // Hitam
  static const Color black = Color(0xff030306);

  // Kuning
  static const Color yellow = Color(0xffEBA91D);

  // Kuning btn
  static const Color yellowBtn = Color(0xffFFC300);

  // Putih
  static const Color white = Color(0xffF3F4EE);

  // Shimmer baseColor
  static const Color baseColor = Color.fromARGB(255, 62, 62, 63);

  // Shimmer highlight color
  static const Color highlightColor = Color.fromARGB(255, 95, 93, 93);
}
